<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_books_fees_search" model="ir.ui.view">
        <field name="model">books.fees</field>
        <field name="arch" type="xml">
            <search>
                <field name="full_name"/>
                <field name="year"/>
                <field name="roll_no"/>
                <field name="standard"/>
                <field name="section"/>
                <field name="contact_no"/>
            </search>
        </field>
    </record>

    <record id="view_book_fees_tree" model="ir.ui.view">
        <field name="model">books.fees</field>
        <field name="arch" type="xml">
            <tree>
                <field name="full_name"/>
                <field name="year"/>
                <field name="roll_no"/>
                <field name="standard"/>
                <field name="section"/>
                <field name="contact_no"/>
            </tree>
        </field>
    </record>


    <record id="view_books_fees_form" model="ir.ui.view">
        <field name="model">books.fees</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="full_name"/>
                            <field name="year"/>
                            <field name="roll_no"/>
                        </group>
                        <group>
                            <field name="standard"/>
                            <field name="section"/>
                            <field name="contact_no"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record model="ir.actions.act_window" id="action_books_fees_list">
        <field name="name">Books Fees</field>
        <field name="res_model">books.fees</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first semester form
            </p>
        </field>
    </record>

</odoo>